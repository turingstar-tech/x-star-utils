import { defineConfig } from 'dumi';

export default defineConfig({
  base: '/x-star-utils/',
  pubilcPath: '/x-star-utils/',
  outputPath: 'docs-dist',
  themeConfig: {
    name: 'x-star-utils',
  },
});
